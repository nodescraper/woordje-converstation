# Base URL of the chat-generation backend.
# Leave empty in dev — Vite proxies /api to http://localhost:5001 (see vite.config.js).
# Set this when hosting the frontend separately from the backend.
VITE_API_BASE=
